# 🗄️ Archive

Older iterations and logs kept for reference.

| File/Folder | Description |
|-------------|-------------|
| `Competitor_Intelligence_Hub/` | Early version of the per-candidate HTML report cards — individual files generated for each of the 28 candidates before the unified report was built. |
| `Older Iterations/` | Previous script versions and early HTML report attempts during development. |
| `errors.log` | Automated error log from scraping runs. Documents which URLs failed, which Google searches hit rate limits, and which candidates returned no data. Useful for debugging future runs. |
